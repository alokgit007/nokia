{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Product",
    "description": "An employee data",
    "type": "object",
    "properties": {
        "name": {
            "description": "Name of the employee",
            "type": "string"
        },
        "id": {
            "type": "number",
            "minimum": 1,
            "exclusiveMinimum": true
        },
        "dob": {
            "description": "date of birth of the employee",
            "type": "date"
        },
        "salary": {
            "type": "number",
            "minimum": 0,
            "exclusiveMinimum": true
        },
        "age": {
            "type": "number",
            "minimum": 0,
            "exclusiveMinimum": true
        }       
    },
    "required": ["name","id","dob", "salary","age"]
}